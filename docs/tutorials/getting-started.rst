Getting started
===============

.. Diátaxis tutorial: one linear path only. Port the golden path from
   pbip-model-forge-docs/tutorials/getting-started.md, reframed for this
   audience per claude-skill-archetype-notes.md: two Claude Code prompts,
   then Power BI Desktop, no shell commands, no flags.

Build your first model
------------------------

.. Show the exact sentence a reader types to invoke create-sample-model,
   for example "create a sample data model for a bakery", and what comes
   back: a working .pbip file plus a plain-language summary of its tables.

Add something to it
---------------------

.. One validate-and-expand pass: adding a measure or a dimension in plain
   language, and the model staying open afterward.

Open it in Power BI Desktop
------------------------------

.. Point at the .pbip file, open it, and look around. This is the last
   step of the tutorial; the next two pages pick up from here.
